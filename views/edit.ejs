<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Your Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center p-6">

  <div class="w-full max-w-md bg-gray-800 p-6 rounded-lg shadow">
    <h1 class="text-xl font-semibold mb-4">Edit Your Post</h1>
<form method="post" action="/update/<%= post._id %>">
    <!-- Simple text box -->
    <textarea
    name="content"
      rows="6"
      class="w-full bg-gray-700 p-3 rounded-md text-gray-100 outline-none mb-4"
      placeholder="Write your updated post here..."
    ><%= post.content %></textarea>

    <!-- Buttons only (no links/actions) -->
    <div class="flex gap-2">
      <button class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-md font-semibold">
        Update Post
      </button>
      <button class="bg-gray-600 hover:bg-gray-700 px-4 py-2 rounded-md font-semibold">
        Cancel
      </button>
    </div>
  </div>
</form>
</body>
</html>
