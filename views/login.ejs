<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <h3 class="text-3xl mb-5">Login</h3>
    <form action="/login" method="post" class="flex flex-col gap-4 max-w-md">
      
      <!-- Username -->
      <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md"
             type="text" placeholder="Username" name="username" required>
      
      <!-- Password -->
      <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md"
             type="password" placeholder="Password" name="password" required>
      
      <!-- Submit Button -->
      <input class="px-5 py-2 bg-blue-500 hover:bg-blue-600 rounded-md cursor-pointer"
             type="submit" value="Login">

    </form>
  </div>
</body>
</html>
