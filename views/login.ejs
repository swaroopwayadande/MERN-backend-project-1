<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white antialiased">
  <main class="min-h-screen w-full flex items-center justify-center p-6">
    <section class="w-full max-w-md">
      <header class="mb-8 text-center">
        <h1 class="text-3xl font-semibold tracking-tight">Login</h1>
        <p class="text-zinc-400 mt-2 text-sm">Welcome back. Enter credentials to continue.</p>
      </header>

      <form action="/login" method="post" class="grid gap-4 bg-zinc-950/40 backdrop-blur rounded-xl p-6 border border-zinc-800">
        <!-- Username -->
        <div class="grid gap-2">
          <label for="username" class="text-sm text-zinc-300">Username</label>
          <input
            id="username"
            name="username"
            type="text"
            placeholder="johndoe"
            required
            autocomplete="username"
            class="px-3 py-2 bg-transparent border-2 border-zinc-800 rounded-md outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/30"
          />
        </div>

        <!-- Password -->
        <div class="grid gap-2">
          <label for="password" class="text-sm text-zinc-300">Password</label>
          <input
            id="password"
            name="password"
            type="password"
            placeholder="••••••••"
            required
            autocomplete="current-password"
            class="px-3 py-2 bg-transparent border-2 border-zinc-800 rounded-md outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/30"
          />
          <div class="flex items-center justify-between">
            <div class="flex items-center gap-2">
              <input id="remember" name="remember" type="checkbox" class="size-4 accent-blue-500" />
              <label for="remember" class="text-xs text-zinc-400 select-none">Remember me</label>
            </div>
            <a href="/forgot-password" class="text-xs text-blue-400 hover:text-blue-300">Forgot password?</a>
          </div>
        </div>

        <!-- Actions -->
        <div class="mt-2 grid gap-3">
          <button
            type="submit"
            class="w-full px-5 py-2.5 bg-blue-500 hover:bg-blue-600 active:bg-blue-700 rounded-md font-medium transition-colors"
          >
            Login
          </button>

          <a
            href="/"
            class="w-full text-center px-5 py-2.5 bg-transparent border-2 border-zinc-700 hover:border-zinc-600 text-zinc-200 rounded-md transition-colors"
          >
            Create account
          </a>
        </div>
      </form>

      <p class="mt-6 text-center text-sm text-zinc-400">
        By continuing, acceptance of the Terms and Privacy Policy is implied.
      </p>
    </section>
  </main>
</body>
</html>
